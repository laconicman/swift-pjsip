.DS_Store
/.build
/.build-pjsip
/Packages
.swiftpm/
xcuserdata/
*.xcodeproj

# The xcframework is distributed as a GitHub Release asset (docs/Versioning.md).
# `scripts/build.sh install` may stage one here for a local path-based test build.
Binaries/
